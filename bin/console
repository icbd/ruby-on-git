#!/usr/bin/env ruby
# frozen_string_literal: true

# Use console script to debug locally through it's absolute path.
$LOAD_PATH << File.expand_path("../lib", __dir__)

require "byebug"
require "bundler/setup"
require "ruby_on_git"

require "irb"
IRB.start(__FILE__)
